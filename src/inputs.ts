type Inputs = {
	fleet: string;
	source: string;
	environment: string;
	balenaToken: string;
	githubToken: string;
	cache: boolean;
	versionbot: boolean;
	createTag: boolean;
};

export default Inputs;
